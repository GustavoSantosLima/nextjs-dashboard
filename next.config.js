/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "https://nextjs-dashboard-coral-omega.vercel.app", "vercel.app"]
  }
};

module.exports = nextConfig;
